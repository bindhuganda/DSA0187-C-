#include<iostream>
using namespace std;
int main()
{
	int n,i,r,sum=0,temp;
	cout<<"enter the number";
	cin>>n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
		
	}
    if(temp==sum)
    {
    	cout<<"armstrong number";
	}
	else
	{
		cout<<" not armstrong number";
	}
	return 0;
}
